This directory is deprecated.

Use the main training pipeline instead:

```
./scripts/run_training_pipeline.sh
```

Or run directly:

```
python -m src.training.run_pipeline --output-dir outputs/train_$(date +%Y%m%d_%H%M)
```
